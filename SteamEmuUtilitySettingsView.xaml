<UserControl x:Class="SteamEmuUtility.SteamEmuUtilitySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Orientation="Vertical">
        <DockPanel Margin="10,10,0,0">
            <TextBlock Text="Load User32.dll"/>
            <Separator Width="40" Background="Transparent"/>
            <TextBlock Text="{Binding Settings.User32Loaded}" Foreground="{Binding Settings.ColorUser32}" Width="400"/>
            <Button Width="80" Content="Browse" Command="{Binding BrowseUser32}"/>
        </DockPanel>
    </StackPanel>
</UserControl>